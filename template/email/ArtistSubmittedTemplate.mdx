import {formatDate} from '@util/date';
export const eventDate = new Date(`${process.env.NEXT_PUBLIC_EVENT_DATE}`);
export const boothFee = process.env.NEXT_PUBLIC_BOOTH_FEE;
export const registrationFeeDate = new Date(`${process.env.NEXT_PUBLIC_REGISTRATION_FEE_DATE}`);
export const notifyStatusDate = new Date(process.env.NEXT_PUBLIC_NOTIFY_DATE);
export const paymentURL = `${process.env.NEXT_PUBLIC_BASE_URL}/payment/booth`;
export const formattedEventDate = formatDate(eventDate);
export const subject = 'Your exhibit has been submitted successfully'; // Email subject string
// ######## Email Template Begins here ########

Dear Artist,

Your application to Gordonston Art Fair for our {formattedEventDate} event has been received.

You will receive an email about your application status on or before {formatDate(notifyStatusDate)}.
The options for outcome are ACCEPTED, DECLINED, or WAITING LIST.

If accepted, you will have until {formatDate(registrationFeeDate)} to pay your booth fee of ${boothFee}.

Sincerely,

The Gordonston Art Fair Committee
